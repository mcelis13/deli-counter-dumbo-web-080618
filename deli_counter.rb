def line(array)
  string = 'The line is currently: '

  if array.size == 0
    puts 'The line is currently empty.'
    return
  end
end

num = 0;
def take_a_number(array, name)
  putsarray.push("Welcome, #{name}. You are #{num} in line.");
end

def now_serving()
end
